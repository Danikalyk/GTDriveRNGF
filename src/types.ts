export type UserListItem = {
  uid: string;
  user: string;
};

export type RouterListItem = {
  uid: string;
  name: string;
  loading_date: string;
  loading_time: string;
  description: string;
  status: string;
};
